pipeline {	
   agent any
   stages {
       stage('Build'){
           steps{
           sh 'echo "Hollo World 2"'
            sh '''
              echo "Multiline shell steps works too"
              ls -lah
             '''

           }
       }
     }
}
